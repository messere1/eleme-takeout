# 第一阶段测试基线报告

## 1. 当前交付状态

- 执行日期：2026-09-01
- 需求基线：SRS V1.1 第一阶段 P0（FR-001～FR-018、FR-020、FR-021）
- 交付角色：测试与迭代适配专员
- 后端测试代码：84 个测试，可正常编译
- 后端红灯结果：通过 26 个，失败 22 个，错误 36 个，跳过 0 个
- 前端测试：3 个，全部通过；当前入口代码行覆盖率 100%
- 核心业务状态：Service 仅保留构造器和公开方法签名，业务方法统一抛出“待功能开发”

结论：测试专员第一阶段交付已经形成可编译的 TDD 红灯基线。后端失败均用于驱动功能开发，不是缺类或测试工程配置错误。FR-019 属于后续阶段，其测试与接口已从本阶段基线移除。

## 2. 验证方式

| 范围 | 命令 | 预期结果 |
| --- | --- | --- |
| 后端测试编译 | `cd backend && mvn test-compile` | 构建成功 |
| 后端红灯基线 | `cd backend && mvn test` | 业务测试失败，指向各 Service 的待实现方法 |
| 前端入口测试 | `cd frontend && npm test` | 3/3 通过 |
| 前端覆盖率 | `cd frontend && npm run coverage` | 当前入口代码 100% |
| 前端构建 | `cd frontend && npm run build` | 构建成功 |

Maven 的失败详情生成在 `backend/target/surefire-reports/`。该目录是可重复生成的本地构建产物，不提交到 Git。


## 3. 功能开发人员接手规则

1. 每次只选择一个 FR 和一个失败场景。
2. 先运行目标测试，确认其因“待功能开发”失败。
3. 编写使该测试通过的最小实现。
4. 单独提交实现，禁止修改测试来规避需求。
5. 运行同模块测试与全量回归，逐步把红灯变为绿灯。

## 4. 第一阶段测试交付边界

本阶段仅交付测试用例、接口契约、测试工程、需求追踪矩阵、回归清单和红灯证据。核心业务实现及尚未开发的前端业务页面由对应功能负责人完成。
