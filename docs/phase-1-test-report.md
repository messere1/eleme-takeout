# 第一阶段回归测试报告

## 1. 当前结论

- 执行日期：2026-09-07
- 需求基线：SRS V1.1 第一阶段 P0（FR-001～FR-018、FR-020、FR-021）
- 交付角色：测试与迭代适配专员
- 后端：113 个测试，103 通过、9 失败、1 错误；测试代码可正常编译
- 前端全量：73 个测试，61 通过、12 失败；其中 5 个失败属于 FR-019/后续商家订单流程，不计入第一阶段验收
- 前端第一阶段：68 个相关测试，61 通过、7 失败；本轮补充的 5 个场景中 3 通过、2 失败
- 本轮变更范围：仅测试代码和测试文档，未修改核心业务实现

第一阶段的测试体系已覆盖主要正常、异常、边界和权限场景，但当前版本尚未达到“全量回归通过”的验收条件。失败项已转录到《第一阶段回归缺陷清单》，应由相应功能负责人修复实现后再次回归，不能通过放宽测试规避。

本轮缺口审计累计补充 34 个测试执行项：后端 29 个，覆盖 DTO 边界、重复手机号、错误密码、资源不存在、资源归属、空购物车、跨店下单、下架商品、分页边界及非法订单访问角色；前端 5 个，覆盖删除非空分类、购物车更新失败、停业店铺加购、订单列表加载失败和订单详情加载失败。

## 2. 验证记录

| 范围 | 命令 | 实际结果 |
| --- | --- | --- |
| 后端测试编译 | `cd backend && mvn test-compile` | 成功 |
| 后端全量回归 | `cd backend && mvn test` | 113：103 通过、9 失败、1 错误 |
| 前端全量回归 | `cd frontend && npm test` | 73：61 通过、12 失败；第一阶段相关失败 7 个 |
| 本轮新增前端场景 | 运行 5 个相关测试文件 | 30：28 通过、2 失败，另发现 1 个未处理异常 |

后端详细结果生成在 `backend/target/surefire-reports/`。该目录属于可重复生成的本地构建产物，不提交到 Git。

## 3. 尚缺或未闭环的覆盖

1. 商家商品新增、编辑、删除、上下架和库存管理页面已有 5 个自动化测试，但页面仍是空桩，尚未闭环。
2. 当前没有真实数据库与浏览器端到端联调测试，现阶段主要是 Service、Controller 契约和 Vue 组件测试。
3. 覆盖率验收需等待现有失败项修复后重新生成，不能以红灯测试下的覆盖率作为阶段通过依据。
4. `MerchantOrders.test.js` 的 4 个用例和 `Orders.test.js` 的取消订单用例属于后续阶段红灯基线，应单独跟踪，不计入第一阶段通过率。

## 4. 后续回归规则

1. 功能负责人每修复一个缺陷，先运行对应失败测试，再运行同模块测试。
2. 修复提交不得删除、跳过或弱化既有断言；若契约确需变更，应先评审并同步修改 SRS、接口文档和测试。
3. 最终执行前后端全量回归，目标为零失败、零未处理异常。
4. 测试专员只维护用例、回归证据、缺陷和需求影响记录，不代写核心业务实现。
