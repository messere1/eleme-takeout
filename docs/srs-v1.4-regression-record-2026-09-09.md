# SRS V1.4 测试与回归记录

## 1. 执行信息

- 日期：2026-09-09
- 代码基线：`2e31f12`
- 需求基线：SRS V1.4
- 执行范围：测试专员职责内的契约分析、测试补充、回归与记录
- 业务代码变更：无

## 2. 补充前基线结果

| 范围 | 结果 | 说明 |
| --- | --- | --- |
| 后端全量 | 167 项，164 通过、3 失败 | 失败均为既有安全红灯：通用 500 兜底缺失、注册 DTO 暴露密码、注册 DTO 暴露完整手机号 |
| 前端全量 | 95 项，94 通过、1 超时 | `main.test.js` 在本机并行全量运行中超过 40 秒；其余测试通过，属于需复跑确认的稳定性问题 |

## 3. V1.4 新增红灯

新增目标测试执行结果：

| 范围 | 结果 |
| --- | --- |
| 后端 `SrsV14ApiContractTest` | 3 项：1 通过、2 红灯 |
| 前端 SRS/Cart/Orders/管理员路由目标回归 | 31 项：28 通过、3 红灯 |

订单接单、完成、确认收货路径与顾客确认页面用例均通过；红灯只指向管理员模块和收货快照缺口。

| 缺陷编号 | 对应需求 | 现状 | 风险 | 关闭标准 |
| --- | --- | --- | --- | --- |
| V14-BE-001 | FR-024 | `CreateOrderRequest` 只有 `address` | 无法保存收货人和联系电话快照，历史订单信息不完整 | 后端相关契约、校验、持久化与查询测试全部通过 |
| V14-BE-002 | FR-023 | 无 `ADMIN` 管理员只读接口 | 评审要求的涉众与用例无可验证实现 | 两个分页 GET 接口通过，写操作保持禁止 |
| V14-FE-001 | FR-024 | `createOrder` 丢弃请求参数；购物车仅有地址输入 | 前后端即使扩展也无法完成联调 | API 契约和购物车三字段用例通过 |

## 4. 已确认覆盖

- FR-022 三个动作接口路径存在。
- 商家只能处理本店订单，顾客只能确认本人订单；非法状态和并发条件更新已有服务测试。
- 商家订单页已有接单、完成动作；顾客订单页已有确认动作。
- 支付、超时取消、部分退款、图片上传、账号注销、管理员写权限、配送均未纳入当前验收，不应据此判定本阶段缺陷。

## 5. 当前结论

SRS V1.4 变更测试已进入 TDD 红灯阶段。现阶段不能标记 V1.4 全量验收通过；应先保留本次测试提交，再由功能负责人用独立提交完成 FR-023、FR-024 最小实现，之后由测试专员复跑全量回归并更新本记录。
